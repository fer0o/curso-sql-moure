-- comando para selecionar todos os registros da tabela users
SELECT * FROM users;
-- comando para selecionar nombre de los usuarios
SELECT name FROM users;
--comando para obtener user_id y name de los usuarios
SELECT user_id, name FROM users;
--comando para obtener user_id, name y email de los usuarios
SELECT user_id, name, email FROM users;
